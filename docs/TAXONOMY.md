# Taxonomy

Generated from `src/data/taxonomy/` by `scripts/generate-taxonomy-doc.ts`. Do not hand-edit.

Legend: ○ skeleton · ◐ in-progress · ● complete

- **384** total nodes (**27** complete, **357** skeleton)
- **59** problem shapes
- **964** relationships
- **6** complexity heuristics

### advanced-graph-algorithms

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `articulation-points` | Articulation Points | algorithm | advanced | medium |
| ○ | `biconnected-components` | Biconnected Components | concept | specialist | low |
| ○ | `binary-lifting-graphs` | Binary Lifting on Graphs | algorithm | advanced | low |
| ○ | `bridges` | Bridges (Cut Edges) | algorithm | advanced | medium |
| ○ | `cycle-jumping` | Cycle Jumping | pattern | advanced | low |
| ○ | `eulerian-circuit` | Eulerian Circuit | concept | advanced | low |
| ○ | `eulerian-path` | Eulerian Path | concept | advanced | low |
| ○ | `functional-graphs` | Functional Graphs | concept | advanced | low |
| ○ | `graph-condensation` | Graph Condensation | concept | advanced | low |
| ○ | `hamiltonian-path` | Hamiltonian Path | concept | specialist | low |
| ○ | `hierholzers-algorithm` | Hierholzer's Algorithm | algorithm | advanced | low |
| ○ | `kosaraju` | Kosaraju's Algorithm | algorithm | advanced | low |
| ○ | `strongly-connected-components` | Strongly Connected Components | concept | advanced | medium |
| ○ | `tarjan-scc` | Tarjan's SCC Algorithm | algorithm | specialist | low |
| ○ | `transitive-closure` | Transitive Closure | concept | advanced | low |

### advanced-search

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `beam-search` | Beam Search | concept | advanced | low |
| ○ | `bidirectional-search` | Bidirectional Search | pattern | advanced | low |
| ○ | `iterative-deepening-dfs` | Iterative Deepening DFS | algorithm | advanced | low |
| ○ | `meet-in-the-middle` | Meet in the Middle | strategy | advanced | medium |
| ○ | `uniform-cost-search` | Uniform Cost Search | algorithm | intermediate | medium |

### advanced-trees

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `binary-lifting` | Binary Lifting | algorithm | advanced | medium |
| ○ | `centroid-decomposition` | Centroid Decomposition | algorithm | specialist | low |
| ○ | `dsu-on-tree` | DSU on Tree / Small-to-Large | algorithm | specialist | low |
| ○ | `euler-tour` | Euler Tour | pattern | advanced | medium |
| ○ | `heavy-light-decomposition` | Heavy-Light Decomposition | algorithm | specialist | low |
| ○ | `lca-binary-lifting` | LCA with Binary Lifting | algorithm | advanced | medium |
| ○ | `rerooting-dp` | Rerooting DP | pattern | specialist | low |
| ○ | `tree-dp` | Tree DP | pattern | advanced | high |
| ○ | `tree-flattening` | Tree Flattening | pattern | intermediate | medium |
| ○ | `virtual-tree` | Virtual Tree | concept | specialist | low |

### algorithmic-techniques

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `coordinate-compression` | Coordinate Compression | pattern | intermediate | medium |
| ● | `cyclic-sort` | Cyclic Sort | pattern | intermediate | medium |
| ○ | `difference-array` | Difference Array | pattern | intermediate | medium |
| ○ | `dutch-national-flag` | Dutch National Flag | algorithm | intermediate | medium |
| ● | `fast-slow-pointers` | Fast/Slow Pointers | pattern | core | high |
| ○ | `fixed-sliding-window` | Fixed Sliding Window | pattern | core | high |
| ● | `frequency-counting` | Frequency Counting | pattern | beginner | very-high |
| ○ | `index-placement` | Index Placement | pattern | intermediate | medium |
| ● | `kadanes-algorithm` | Kadane's Algorithm | algorithm | core | very-high |
| ○ | `linear-scan` | Linear Scan | pattern | beginner | high |
| ○ | `opposite-direction-two-pointers` | Opposite-Direction Two Pointers | pattern | core | high |
| ○ | `partitioning` | Partitioning | pattern | core | high |
| ○ | `prefix-product` | Prefix Product | pattern | intermediate | medium |
| ● | `prefix-sum` | Prefix Sum | pattern | core | very-high |
| ○ | `read-write-pointer` | Read/Write Pointer | pattern | core | medium |
| ○ | `running-sum` | Running Sum | pattern | beginner | medium |
| ○ | `same-direction-two-pointers` | Same-Direction Two Pointers | pattern | core | high |
| ● | `sliding-window` | Sliding Window | pattern | core | very-high |
| ○ | `sort-and-scan` | Sorting + Scan | pattern | beginner | high |
| ○ | `suffix-sum` | Suffix Sum | pattern | core | medium |
| ● | `two-pointers` | Two Pointers | pattern | core | very-high |
| ○ | `variable-sliding-window` | Variable Sliding Window | pattern | core | very-high |

### bit-manipulation

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `bit-manipulation-primitives` | Bit Manipulation Primitives (Set / Clear / Toggle / Check Bit) | pattern | core | very-high |
| ○ | `bit-shifting` | Bit Shifting (Left Shift / Right Shift) | concept | beginner | high |
| ○ | `bitmask-enumeration` | Bitmask Enumeration | pattern | intermediate | high |
| ○ | `bitset-techniques` | Bitset Techniques | concept | advanced | medium |
| ○ | `bitwise-operators` | Bitwise Operators (AND / OR / XOR / NOT) | concept | beginner | very-high |
| ○ | `gray-code` | Gray Code | concept | advanced | low |
| ○ | `lowest-set-bit` | Lowest Set Bit | pattern | core | high |
| ○ | `power-of-two-check` | Power-of-Two Check | pattern | beginner | high |
| ○ | `submask-enumeration` | Submask Enumeration | pattern | advanced | medium |
| ○ | `xor-cancellation` | XOR Cancellation | concept | core | high |

### competitive-programming-techniques

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `bitset-optimization` | Bitset Optimization | optimization | specialist | low |
| ○ | `broken-profile-dp` | Broken Profile DP | pattern | specialist | low |
| ○ | `difference-constraints` | Difference Constraints | concept | specialist | low |
| ○ | `dsu-rollback` | DSU Rollback | optimization | specialist | low |
| ○ | `li-chao-tree` | Li Chao Tree | data-structure | specialist | low |
| ○ | `persistent-data-structures` | Persistent Data Structures | concept | specialist | low |
| ○ | `persistent-segment-tree` | Persistent Segment Tree | data-structure | specialist | low |
| ○ | `small-to-large-merging` | Small-to-Large Merging | algorithm | specialist | low |
| ○ | `sos-dp` | SOS DP / Sum over Subsets | pattern | specialist | low |
| ○ | `treap` | Treap | data-structure | specialist | low |

### computational-geometry

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `collinearity` | Collinearity | concept | core | low |
| ○ | `convex-hull` | Convex Hull | algorithm | advanced | medium |
| ○ | `cross-product` | Cross Product | concept | core | low |
| ○ | `geometry-distance` | Distance | concept | beginner | medium |
| ○ | `dot-product` | Dot Product | concept | core | low |
| ○ | `graham-scan` | Graham Scan | algorithm | advanced | low |
| ○ | `line-intersection` | Line Intersection | algorithm | advanced | low |
| ○ | `monotonic-chain` | Monotonic Chain | algorithm | advanced | low |
| ○ | `orientation` | Orientation | concept | core | low |
| ○ | `point-in-polygon` | Point in Polygon | algorithm | advanced | low |
| ○ | `points-and-vectors` | Points and Vectors | concept | beginner | low |
| ○ | `polygon-area` | Polygon Area | algorithm | intermediate | low |
| ○ | `segment-intersection` | Segment Intersection | algorithm | advanced | low |
| ○ | `shoelace-formula` | Shoelace Formula | algorithm | intermediate | low |
| ○ | `sweep-line-geometry` | Sweep Line Geometry | pattern | advanced | low |

### connectivity

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `kruskal-integration` | Kruskal Integration | concept | advanced | medium |
| ○ | `offline-connectivity` | Offline Connectivity | pattern | advanced | medium |
| ○ | `path-compression` | Path Compression | optimization | intermediate | medium |
| ○ | `union-by-rank` | Union by Rank | optimization | intermediate | medium |
| ○ | `union-by-size` | Union by Size | optimization | intermediate | medium |
| ● | `union-find` | Union Find | data-structure | core | very-high |

### data-structures

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `array` | Array | data-structure | beginner | very-high |
| ○ | `counting-array` | Counting Array | data-structure | beginner | medium |
| ○ | `graph` | Graph | data-structure | core | very-high |
| ○ | `hash-map` | Hash Map | data-structure | beginner | very-high |
| ○ | `hash-set` | Hash Set | data-structure | beginner | very-high |
| ● | `linked-list` | Linked List | data-structure | beginner | high |

### directed-graphs

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `dag-dynamic-programming` | DAG Dynamic Programming | pattern | advanced | medium |
| ○ | `dag-processing` | DAG Processing | concept | intermediate | medium |
| ○ | `dependency-resolution` | Dependency Resolution | pattern | intermediate | high |
| ○ | `dfs-topological-sort` | DFS Topological Sort | algorithm | intermediate | high |
| ○ | `directed-cycle-detection` | Directed Cycle Detection | pattern | intermediate | high |
| ○ | `indegree` | Indegree | concept | beginner | medium |
| ○ | `kahns-algorithm` | Kahn's Algorithm | algorithm | intermediate | high |
| ● | `topological-sort` | Topological Sort | pattern | intermediate | very-high |

### divide-and-conquer

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `closest-pair` | Closest Pair of Points | algorithm | advanced | low |
| ○ | `divide-and-conquer` | Divide and Conquer | strategy | core | very-high |
| ○ | `recursive-partitioning` | Recursive Partitioning | concept | core | medium |

### dynamic-programming

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ● | `01-knapsack` | 0/1 Knapsack | pattern | core | very-high |
| ○ | `1d-dp` | 1D DP | pattern | core | very-high |
| ○ | `2d-dp` | 2D DP | pattern | core | very-high |
| ○ | `base-case` | Base Case | concept | core | high |
| ○ | `bitmask-dp` | Bitmask DP | pattern | advanced | medium |
| ○ | `bounded-knapsack` | Bounded Knapsack | pattern | intermediate | medium |
| ○ | `coin-change` | Coin Change | pattern | core | very-high |
| ○ | `convex-hull-trick` | Convex Hull Trick | optimization | specialist | low |
| ○ | `digit-dp` | Digit DP | pattern | specialist | low |
| ○ | `divide-and-conquer-optimization` | Divide and Conquer Optimization | optimization | specialist | low |
| ○ | `dp-optimization` | DP Optimization | optimization | advanced | low |
| ○ | `dp-state` | DP State | concept | core | very-high |
| ○ | `dp-transition` | DP Transition | concept | core | very-high |
| ○ | `dp-monotonic-queue` | DP with Monotonic Queue | pattern | advanced | low |
| ○ | `dynamic-programming` | Dynamic Programming | strategy | core | very-high |
| ○ | `edit-distance` | Edit Distance | algorithm | core | very-high |
| ○ | `expected-value-dp` | Expected Value DP | pattern | specialist | low |
| ○ | `game-dp` | Game DP | pattern | specialist | low |
| ○ | `grid-dp` | Grid DP | pattern | core | very-high |
| ○ | `interval-dp` | Interval DP | pattern | advanced | medium |
| ○ | `iteration-order` | Iteration Order | concept | intermediate | high |
| ○ | `knuth-optimization` | Knuth's Optimization | optimization | specialist | low |
| ○ | `longest-common-subsequence` | Longest Common Subsequence | algorithm | core | very-high |
| ○ | `longest-increasing-subsequence` | Longest Increasing Subsequence | algorithm | core | very-high |
| ○ | `memoization` | Memoization | concept | core | very-high |
| ○ | `palindrome-dp` | Palindrome DP | pattern | intermediate | high |
| ○ | `partition-dp` | Partition DP | pattern | core | high |
| ○ | `prefix-sum-optimization` | Prefix-Sum Optimization | optimization | intermediate | medium |
| ○ | `probability-dp` | Probability DP | pattern | specialist | low |
| ○ | `regex-wildcard-matching-dp` | Regex / Wildcard Matching DP | pattern | advanced | medium |
| ○ | `state-compression` | State Compression | concept | intermediate | medium |
| ○ | `state-machine-dp` | State Machine DP | pattern | intermediate | high |
| ○ | `stock-dp` | Stock DP | pattern | intermediate | high |
| ○ | `subset-sum` | Subset Sum | pattern | core | very-high |
| ○ | `tabulation` | Tabulation | concept | core | very-high |
| ○ | `unbounded-knapsack` | Unbounded Knapsack | pattern | core | high |
| ○ | `word-break` | Word Break | pattern | core | high |

### game-theory

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `alpha-beta-pruning` | Alpha-Beta Pruning | optimization | advanced | low |
| ○ | `grundy-numbers` | Grundy Numbers | concept | specialist | low |
| ○ | `minimax` | Minimax | algorithm | advanced | low |
| ○ | `nim` | Nim | concept | advanced | low |
| ○ | `sprague-grundy-theorem` | Sprague-Grundy Theorem | concept | specialist | low |
| ○ | `xor-game-theory` | XOR Game Theory | concept | specialist | low |

### graph-traversal

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `bidirectional-bfs` | Bidirectional BFS | pattern | advanced | medium |
| ○ | `bipartite-checking` | Bipartite Checking | pattern | intermediate | high |
| ● | `bfs` | Breadth-First Search | pattern | core | very-high |
| ○ | `component-labeling` | Component Labeling | pattern | core | medium |
| ○ | `connected-components` | Connected Components | pattern | core | very-high |
| ○ | `graph-cycle-detection` | Cycle Detection in Graphs | pattern | intermediate | high |
| ● | `dfs` | Depth-First Search | pattern | core | very-high |
| ○ | `flood-fill` | Flood Fill | pattern | core | high |
| ○ | `graph-coloring` | Graph Coloring | pattern | advanced | medium |
| ○ | `grid-graph` | Grid as a Graph | concept | core | very-high |
| ○ | `multi-source-bfs` | Multi-source BFS | pattern | intermediate | high |

### greedy

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `activity-selection` | Activity Selection | pattern | core | high |
| ○ | `deadline-scheduling` | Deadline Scheduling | pattern | intermediate | medium |
| ○ | `earliest-finish-time` | Earliest Finish Time | concept | core | high |
| ○ | `exchange-argument` | Exchange Argument | concept | advanced | low |
| ○ | `fractional-knapsack` | Fractional Knapsack | algorithm | core | medium |
| ○ | `gas-station` | Gas Station | pattern | intermediate | medium |
| ○ | `greedy` | Greedy | strategy | core | very-high |
| ○ | `greedy-graph-algorithms` | Greedy Graph Algorithms | concept | core | high |
| ○ | `huffman-coding` | Huffman Coding | algorithm | advanced | low |
| ○ | `interval-scheduling` | Interval Scheduling | pattern | core | very-high |
| ○ | `jump-game` | Jump Game | pattern | core | high |
| ○ | `local-optimal-choice` | Local Optimal Choice | concept | core | high |
| ○ | `sorting-plus-greedy` | Sorting + Greedy | pattern | core | very-high |

### heaps

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `best-first-search` | Best-First Search | algorithm | advanced | medium |
| ○ | `heap` | Heap | data-structure | core | very-high |
| ○ | `heap-based-kth-element` | Heap-based Kth Element | pattern | core | high |
| ○ | `indexed-heap` | Indexed Heap | concept | advanced | low |
| ○ | `lazy-deletion-heap` | Lazy Deletion Heap | optimization | advanced | low |
| ○ | `max-heap` | Max Heap | data-structure | core | very-high |
| ○ | `merge-k-sorted-arrays` | Merge K Sorted Arrays | pattern | intermediate | medium |
| ● | `merge-k-sorted-lists` | Merge K Sorted Lists | pattern | intermediate | high |
| ● | `min-heap` | Min Heap | data-structure | core | very-high |
| ○ | `scheduling-with-heap` | Scheduling with Heap | pattern | intermediate | medium |
| ○ | `streaming-median` | Streaming Median | pattern | advanced | medium |
| ● | `two-heaps` | Two Heaps | pattern | advanced | medium |

### intervals-events

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `calendar-booking` | Calendar Booking | pattern | intermediate | medium |
| ○ | `difference-events` | Difference Events | pattern | intermediate | medium |
| ○ | `event-sorting` | Event Sorting | concept | core | medium |
| ○ | `insert-interval` | Insert Interval | pattern | core | high |
| ○ | `interval-intersection` | Interval Intersection | pattern | intermediate | medium |
| ○ | `maximum-concurrent-events` | Maximum Concurrent Events | pattern | intermediate | medium |
| ○ | `meeting-rooms` | Meeting Rooms | pattern | core | high |
| ● | `merge-intervals` | Merge Intervals | pattern | core | very-high |
| ○ | `non-overlapping-intervals` | Non-overlapping Intervals | pattern | intermediate | high |
| ○ | `sweep-line` | Sweep Line | pattern | advanced | medium |

### linked-lists

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `clone-linked-structure` | Clone Linked Structure | pattern | advanced | high |
| ○ | `cycle-detection` | Cycle Detection | pattern | core | very-high |
| ○ | `dummy-node` | Dummy Node | pattern | beginner | very-high |
| ○ | `find-cycle-start` | Find Cycle Start | pattern | intermediate | high |
| ○ | `floyds-algorithm` | Floyd's Cycle-Finding Algorithm | algorithm | core | very-high |
| ○ | `in-place-pointer-manipulation` | In-place Pointer Manipulation | concept | core | high |
| ○ | `intersection-of-linked-lists` | Intersection of Linked Lists | pattern | intermediate | high |
| ○ | `merge-lists` | Merge Lists | pattern | core | very-high |
| ○ | `middle-node` | Middle Node | pattern | beginner | very-high |
| ○ | `reordering` | Reordering | pattern | intermediate | medium |
| ● | `reverse-list` | Reverse List | pattern | beginner | very-high |
| ○ | `reverse-sublist` | Reverse Sublist | pattern | intermediate | high |
| ○ | `sentinel-node` | Sentinel Node | pattern | beginner | medium |
| ○ | `split-list` | Split List | pattern | intermediate | medium |

### mathematical-algorithms

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `binary-exponentiation` | Binary Exponentiation (Fast Exponentiation) | algorithm | core | very-high |
| ○ | `binomial-coefficients` | Binomial Coefficients | concept | intermediate | medium |
| ○ | `catalan-numbers` | Catalan Numbers | concept | advanced | medium |
| ○ | `chinese-remainder-theorem` | Chinese Remainder Theorem | algorithm | specialist | low |
| ○ | `combinatorics-counting` | Combinations / Permutations Counting | concept | intermediate | high |
| ○ | `divisor-enumeration` | Divisor Enumeration | pattern | core | medium |
| ○ | `euclidean-algorithm` | Euclidean Algorithm | algorithm | beginner | very-high |
| ○ | `eulers-totient-function` | Euler's Totient Function | concept | advanced | low |
| ○ | `extended-euclidean-algorithm` | Extended Euclidean Algorithm | algorithm | advanced | medium |
| ○ | `fermats-little-theorem` | Fermat's Little Theorem | concept | advanced | medium |
| ○ | `fibonacci-fast-doubling` | Fibonacci Fast Doubling | algorithm | advanced | low |
| ○ | `gcd` | GCD (Greatest Common Divisor) | algorithm | beginner | very-high |
| ○ | `inclusion-exclusion-principle` | Inclusion-Exclusion Principle | concept | advanced | medium |
| ○ | `lcm` | LCM (Least Common Multiple) | concept | beginner | high |
| ○ | `linear-sieve` | Linear Sieve | algorithm | advanced | low |
| ○ | `matrix-exponentiation` | Matrix Exponentiation | algorithm | advanced | medium |
| ○ | `modular-arithmetic` | Modular Arithmetic | concept | core | very-high |
| ○ | `modular-combinations` | Modular Combinations | concept | advanced | medium |
| ○ | `modular-inverse` | Modular Inverse | concept | advanced | medium |
| ○ | `pascals-triangle` | Pascal's Triangle | concept | beginner | medium |
| ○ | `pigeonhole-principle` | Pigeonhole Principle | concept | intermediate | medium |
| ○ | `prime-factorization` | Prime Factorization | algorithm | core | high |
| ○ | `prime-testing` | Prime Testing | algorithm | core | high |
| ○ | `sieve-of-eratosthenes` | Sieve of Eratosthenes | algorithm | core | very-high |

### matrix-grid

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `boundary-traversal` | Boundary Traversal | pattern | core | low |
| ○ | `direction-arrays` | Direction Arrays | concept | beginner | very-high |
| ○ | `matrix-bfs` | Matrix BFS | pattern | core | very-high |
| ○ | `matrix-dfs` | Matrix DFS | pattern | core | very-high |
| ○ | `matrix-dp` | Matrix DP | pattern | core | high |
| ○ | `prefix-matrix` | Prefix Matrix / 2D Prefix Sum | pattern | core | high |
| ○ | `matrix-rotation` | Rotation | pattern | core | medium |
| ○ | `spiral-matrix` | Spiral Matrix | pattern | core | high |
| ○ | `matrix-transpose` | Transpose | pattern | beginner | medium |

### minimum-spanning-tree

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ● | `kruskal` | Kruskal's Algorithm | algorithm | intermediate | high |
| ○ | `minimum-spanning-forest` | Minimum Spanning Forest | concept | advanced | medium |
| ○ | `prim` | Prim's Algorithm | algorithm | intermediate | high |

### network-flow

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `assignment-problem` | Assignment Problem | concept | advanced | low |
| ○ | `bipartite-matching` | Bipartite Matching | pattern | advanced | medium |
| ○ | `dinic` | Dinic's Algorithm | algorithm | specialist | low |
| ○ | `edmonds-karp` | Edmonds-Karp Algorithm | algorithm | advanced | low |
| ○ | `ford-fulkerson` | Ford-Fulkerson Method | algorithm | advanced | low |
| ○ | `hopcroft-karp` | Hopcroft-Karp Algorithm | algorithm | specialist | low |
| ○ | `hungarian-algorithm` | Hungarian Algorithm | algorithm | specialist | low |
| ○ | `kuhns-algorithm` | Kuhn's Algorithm | algorithm | advanced | low |
| ○ | `maximum-flow` | Maximum Flow | concept | advanced | medium |
| ○ | `min-cost-max-flow` | Min-Cost Max-Flow | algorithm | specialist | low |
| ○ | `minimum-cut` | Minimum Cut | concept | advanced | low |

### offline-query-processing

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `offline-dsu` | Offline DSU | pattern | specialist | low |
| ○ | `offline-processing` | Offline Processing | strategy | advanced | medium |
| ○ | `parallel-binary-search` | Parallel Binary Search | algorithm | specialist | low |
| ○ | `sort-queries` | Sort Queries | concept | intermediate | medium |

### randomized-algorithms

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `fisher-yates-shuffle` | Fisher-Yates Shuffle | algorithm | core | medium |
| ○ | `hashing-concepts` | Hashing Concepts | concept | intermediate | medium |
| ○ | `monte-carlo-vs-las-vegas` | Monte Carlo vs Las Vegas | concept | advanced | low |
| ○ | `randomized-quickselect` | Randomized Quickselect | algorithm | intermediate | high |
| ○ | `randomized-quicksort` | Randomized Quicksort | algorithm | intermediate | medium |
| ○ | `reservoir-sampling` | Reservoir Sampling | algorithm | advanced | low |

### range-query-techniques

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `fenwick-tree` | Fenwick Tree / Binary Indexed Tree | data-structure | intermediate | high |
| ○ | `lazy-segment-tree` | Lazy Segment Tree | data-structure | advanced | medium |
| ○ | `mos-algorithm` | Mo's Algorithm | algorithm | specialist | low |
| ○ | `ordered-set` | Ordered Set / Order-Statistics Structure | data-structure | advanced | medium |
| ○ | `segment-tree` | Segment Tree | data-structure | intermediate | high |
| ○ | `sparse-table` | Sparse Table | data-structure | intermediate | medium |
| ○ | `sqrt-decomposition` | Square Root Decomposition | data-structure | advanced | low |

### recursion-backtracking

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `backtracking` | Backtracking | strategy | core | very-high |
| ○ | `basic-recursion` | Basic Recursion | concept | beginner | very-high |
| ○ | `branch-and-bound` | Branch and Bound | strategy | advanced | low |
| ○ | `choose-explore-undo` | Choose / Explore / Undo | concept | core | very-high |
| ○ | `combination-sum` | Combination Sum | pattern | core | high |
| ○ | `combinations` | Combinations | pattern | core | high |
| ○ | `constraint-search` | Constraint Search | concept | intermediate | medium |
| ○ | `n-queens` | N-Queens | pattern | intermediate | high |
| ○ | `permutations` | Permutations | pattern | core | high |
| ○ | `pruning` | Pruning | optimization | intermediate | high |
| ○ | `recursion` | Recursion | concept | beginner | very-high |
| ○ | `sudoku-solver` | Sudoku Solver | pattern | advanced | medium |
| ○ | `word-search` | Word Search | pattern | intermediate | very-high |

### shortest-paths

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `0-1-bfs` | 0-1 BFS | algorithm | advanced | medium |
| ○ | `a-star` | A* Search | algorithm | advanced | low |
| ○ | `bellman-ford` | Bellman-Ford Algorithm | algorithm | advanced | high |
| ○ | `bfs-shortest-path` | BFS Shortest Path | pattern | intermediate | very-high |
| ○ | `dag-shortest-path` | DAG Shortest Path | algorithm | advanced | medium |
| ● | `dijkstra` | Dijkstra's Algorithm | algorithm | intermediate | very-high |
| ○ | `floyd-warshall` | Floyd-Warshall Algorithm | algorithm | advanced | high |
| ○ | `johnsons-algorithm` | Johnson's Algorithm | algorithm | specialist | low |
| ○ | `multi-source-shortest-path` | Multi-source Shortest Path | pattern | advanced | medium |

### sorting-searching

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `binary-search` | Binary Search | pattern | core | very-high |
| ○ | `binary-search-on-answer` | Binary Search on Answer | pattern | advanced | very-high |
| ○ | `bubble-sort` | Bubble Sort | algorithm | beginner | low |
| ○ | `bucket-sort` | Bucket Sort | algorithm | intermediate | medium |
| ○ | `counting-sort` | Counting Sort | algorithm | intermediate | medium |
| ○ | `custom-comparator` | Custom Comparator | pattern | core | high |
| ○ | `first-occurrence` | First Occurrence | pattern | core | high |
| ○ | `heap-sort` | Heap Sort | algorithm | intermediate | high |
| ● | `insertion-sort` | Insertion Sort | algorithm | beginner | medium |
| ○ | `last-occurrence` | Last Occurrence | pattern | core | high |
| ○ | `lower-bound` | Lower Bound | algorithm | core | high |
| ○ | `merge-sort` | Merge Sort | algorithm | core | very-high |
| ○ | `monotonic-predicate-search` | Monotonic Predicate Search | pattern | advanced | medium |
| ○ | `parametric-search` | Parametric Search | algorithm | advanced | low |
| ○ | `partial-sorting` | Partial Sorting | pattern | intermediate | medium |
| ○ | `peak-finding` | Peak Finding | pattern | intermediate | high |
| ○ | `quick-sort` | Quick Sort | algorithm | core | very-high |
| ○ | `quickselect` | Quickselect | algorithm | intermediate | high |
| ○ | `radix-sort` | Radix Sort | algorithm | intermediate | medium |
| ○ | `rotated-array-search` | Rotated Array Search | pattern | intermediate | very-high |
| ○ | `search-in-matrix` | Search in Matrix | pattern | intermediate | high |
| ○ | `search-insert-position` | Search Insert Position | pattern | beginner | medium |
| ○ | `selection-sort` | Selection Sort | algorithm | beginner | low |
| ○ | `shell-sort` | Shell Sort | algorithm | intermediate | low |
| ○ | `sorting` | Sorting | concept | beginner | very-high |
| ○ | `stable-vs-unstable-sorting` | Stable vs. Unstable Sorting | concept | core | medium |
| ● | `standard-binary-search` | Standard Binary Search | algorithm | beginner | very-high |
| ○ | `ternary-search` | Ternary Search | algorithm | advanced | medium |
| ○ | `three-way-partitioning` | Three-Way Partitioning | pattern | intermediate | medium |
| ○ | `timsort` | TimSort | concept | advanced | low |
| ○ | `upper-bound` | Upper Bound | algorithm | core | high |

### stacks-queues

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `circular-queue` | Circular Queue | data-structure | core | medium |
| ○ | `deque` | Deque | data-structure | core | high |
| ○ | `expression-parsing` | Expression Parsing | pattern | intermediate | medium |
| ○ | `infix-postfix-evaluation` | Infix/Postfix Evaluation | pattern | intermediate | medium |
| ○ | `max-stack` | Max Stack | data-structure | core | medium |
| ○ | `min-stack` | Min Stack | data-structure | core | high |
| ● | `monotonic-decreasing-stack` | Monotonic Decreasing Stack | pattern | intermediate | very-high |
| ○ | `monotonic-increasing-stack` | Monotonic Increasing Stack | pattern | intermediate | very-high |
| ○ | `monotonic-queue` | Monotonic Queue | pattern | advanced | high |
| ○ | `monotonic-stack` | Monotonic Stack | pattern | intermediate | very-high |
| ● | `queue` | Queue | data-structure | beginner | very-high |
| ○ | `shunting-yard` | Shunting-Yard Algorithm | algorithm | intermediate | medium |
| ● | `stack` | Stack | data-structure | beginner | very-high |
| ○ | `stack-based-parentheses-matching` | Stack-Based Parentheses Matching | pattern | beginner | very-high |
| ○ | `two-stack-techniques` | Two-Stack Techniques | pattern | intermediate | medium |

### string-algorithms

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `aho-corasick` | Aho-Corasick | algorithm | specialist | low |
| ○ | `character-counting` | Character Counting | pattern | beginner | very-high |
| ○ | `expand-around-center` | Expand Around Center | pattern | core | very-high |
| ○ | `kmp` | KMP (Knuth-Morris-Pratt) | algorithm | intermediate | high |
| ○ | `lcp-array` | LCP Array | concept | advanced | low |
| ○ | `manachers-algorithm` | Manacher's Algorithm | algorithm | advanced | medium |
| ○ | `palindromic-tree` | Palindromic Tree | data-structure | specialist | low |
| ○ | `prefix-function` | Prefix Function | concept | intermediate | medium |
| ○ | `prefix-trie` | Prefix Trie | data-structure | core | medium |
| ○ | `rabin-karp` | Rabin-Karp | algorithm | intermediate | medium |
| ○ | `rolling-hash` | Rolling Hash | concept | core | high |
| ○ | `suffix-array` | Suffix Array | data-structure | advanced | low |
| ○ | `suffix-automaton` | Suffix Automaton | data-structure | specialist | low |
| ○ | `suffix-tree` | Suffix Tree | data-structure | specialist | low |
| ○ | `suffix-trie` | Suffix Trie | concept | specialist | low |
| ○ | `trie` | Trie | data-structure | core | high |
| ○ | `z-algorithm` | Z Algorithm | algorithm | intermediate | medium |

### trees

| | id | name | type | difficulty | interview frequency |
|---|---|---|---|---|---|
| ○ | `binary-search-tree` | Binary Search Tree | data-structure | core | very-high |
| ○ | `bst-insert-delete` | BST Insert/Delete | pattern | intermediate | high |
| ○ | `bst-search` | BST Search | pattern | beginner | high |
| ○ | `bst-validation` | BST Validation | pattern | core | high |
| ○ | `tree-diameter` | Diameter | pattern | intermediate | high |
| ○ | `tree-height-depth` | Height/Depth | concept | beginner | high |
| ○ | `inorder-traversal` | Inorder | pattern | beginner | high |
| ○ | `level-order-traversal` | Level Order | pattern | core | very-high |
| ○ | `lowest-common-ancestor` | Lowest Common Ancestor | algorithm | intermediate | very-high |
| ○ | `tree-order-statistics` | Order Statistics | concept | advanced | medium |
| ○ | `path-sum` | Path Sum | pattern | core | high |
| ○ | `postorder-traversal` | Postorder | pattern | beginner | high |
| ○ | `predecessor-successor` | Predecessor/Successor | pattern | intermediate | medium |
| ○ | `preorder-traversal` | Preorder | pattern | beginner | high |
| ○ | `recursive-tree-state` | Recursive Tree State | concept | core | high |
| ○ | `root-to-leaf-paths` | Root-to-Leaf | pattern | core | high |
| ○ | `subtree-matching` | Subtree Matching | pattern | intermediate | medium |
| ● | `tree-bfs` | Tree BFS | pattern | core | very-high |
| ● | `tree-dfs` | Tree DFS | pattern | core | very-high |
| ○ | `tree-reconstruction` | Tree Reconstruction | pattern | intermediate | medium |
| ○ | `tree-serialization` | Tree Serialization | pattern | intermediate | medium |
| ○ | `zigzag-traversal` | Zigzag Traversal | pattern | intermediate | medium |

## Problem-shape recognition database

Each row is a separate problem shape, with candidate approaches narrowed by constraints.

| Shape | Candidates | Constraint reasoning |
|---|---|---|
| Duplicate Detection (`duplicate-detection`) | hash-set, sort-and-scan, cyclic-sort, fast-slow-pointers | If extra space is unrestricted, a hash set gives O(n) time in one pass. If space must be O(1) and values are guaranteed to lie in [1, n], cyclic sort or index-placement finds the duplicate in place. If values aren't range-bounded but O(1) space is still required, sort first (O(n log n)) and scan for adjacent equals. Fast/slow pointers apply when the array can be treated as an implicit linked list (single duplicate, values in [1, n], array must stay unmodified). |
| Missing Number (`missing-number`) | cyclic-sort, hash-set, running-sum | When values are guaranteed to be exactly the range [0, n] or [1, n] with one missing, cyclic sort (or the sum/XOR formula — an O(1)-space arithmetic shortcut built on running-sum) solves it in O(n) time, O(1) space. A hash set is the general-purpose O(n)-space fallback when the range guarantee doesn't hold or multiple ranges are being compared. |
| Repeating Number (`repeating-number`) | cyclic-sort, index-placement, fast-slow-pointers, hash-set | If array modification is allowed and values sit in [1, n], cyclic sort or index-placement finds the repeat in O(n) time, O(1) space. If the array must stay read-only, Floyd's fast/slow pointers (treating value-at-index as a 'next' pointer) achieves O(1) space without mutation. A hash set is the simplest fallback when neither constraint holds, at the cost of O(n) space. |
| Anagram (`anagram`) | sort-and-scan, frequency-counting, counting-array | Sorting both strings and comparing is O(n log n) per string and simple to write. A frequency array (when the alphabet is small and fixed, e.g. 26 lowercase letters) or a hash map counts each string in O(n) and is preferred at scale or with a large/unicode alphabet. |
| Group Anagrams (`group-anagrams`) | hash-map, sort-and-scan, frequency-counting | Use each string's sorted form (O(n log n) per string) or its character-count signature (O(n) per string, better for long strings or large alphabets) as a hash map key, then bucket strings sharing a key. This is the anagram check applied repeatedly and keyed, rather than compared pairwise. |
| Palindrome (`palindrome`) | opposite-direction-two-pointers, expand-around-center, palindrome-dp, manachers-algorithm | Check an entire string with opposite two pointers in O(n) time and O(1) extra space. For a longest palindromic substring, expand around each center in O(n²), use interval DP, or use Manacher in O(n). A palindromic subsequence permits gaps: center expansion and Manacher do not solve it; use interval DP in O(n²) time. Constraints and required output determine which task you have. |
| Pair Sum (`pair-sum`) | hash-map, opposite-direction-two-pointers | Unsorted input where you must return indices: a hash map gives O(n) time, O(n) space via complement lookup. Sorted input (or sorting is free/already done) where only the values matter: opposite-direction two pointers gives O(n) time, O(1) extra space after the sort. |
| Triplet Sum (`triplet-sum`) | sort-and-scan, opposite-direction-two-pointers, hash-map | Sort the array, then for each fixed first element run opposite-direction two pointers on the remainder: O(n^2) time, O(1) extra space beyond the sort, and sorting also makes duplicate-triplet skipping straightforward. A hash-map-based approach avoids sorting but complicates duplicate handling and is rarely preferred here. |
| K-Sum (`k-sum`) | sort-and-scan, opposite-direction-two-pointers, hash-map | Generalizes triplet-sum by recursively fixing one element at a time (sort once, O(n log n)) until only a pair-sum with two pointers remains as the base case: O(n^(k-1)) time overall. Practical only for small fixed k (3-4); for large or variable k this shape stops being tractable by brute enumeration and signals a different technique (e.g. meet-in-the-middle or subset-sum DP) instead. |
| Kth Largest (`kth-largest`) | heap, quickselect, sort-and-scan | A min-heap of size k gives O(n log k) and adapts well to a data stream. Quickselect gives expected O(n) (worst case O(n^2)) when the array is fully available in memory and only a one-shot answer is needed. Sorting is the simplest O(n log n) fallback. |
| Kth Smallest (`kth-smallest`) | heap, quickselect, sort-and-scan | Same tradeoffs as kth-largest with the comparator flipped: a max-heap of size k for O(n log k) or streaming input, quickselect for expected O(n) one-shot queries, sorting for the simplest O(n log n) fallback. |
| Majority Element (`majority-element`) | linear-scan, frequency-counting, sort-and-scan | Boyer-Moore voting (a specialized linear scan carrying a candidate value + counter) solves the guaranteed-majority (>n/2) case in O(n) time, O(1) space. A hash map/frequency count is the general-purpose O(n) time, O(n) space fallback when there's no guaranteed strict majority (e.g. finding all elements appearing more than n/3 times). Sorting and reading the middle element works in O(n log n) when a strict majority is guaranteed. |
| Frequency Problems (`frequency-problems`) | frequency-counting, heap, counting-array | Build counts with a hash map or counting array (bounded domain) in O(n), then rank or filter: a heap for top-k-by-frequency (O(n log k)), or bucket sort by frequency for O(n) when frequencies are themselves bounded by n (bucket index = frequency). |
| Subarray (`subarray`) | sliding-window, prefix-sum, kadanes-algorithm | Sliding window works when all values are positive, since sums grow monotonically as the window extends. Once negative numbers are allowed, that monotonicity breaks and prefix-sum + hash map is usually necessary for 'sums to k' questions. For the specific ask 'maximum sum contiguous subarray,' Kadane's algorithm is the direct O(n) specialist tool regardless of sign. |
| Subsequence (`subsequence`) | dynamic-programming, binary-search, same-direction-two-pointers | Because elements may be skipped, a sliding window (which requires contiguity) does not apply. Most subsequence optimization problems (longest/shortest satisfying some property) are dynamic programming, O(n^2); some, like longest increasing subsequence, admit an O(n log n) patience-sorting/binary-search refinement. A simple 'is X a subsequence of Y' check is a same-direction two-pointer scan in O(n). |
| Substring (`substring`) | variable-sliding-window, hash-map, kmp, rolling-hash, palindrome-dp | Substring means contiguous characters, not a particular algorithm. A character-count condition whose validity supports safe grow/shrink decisions suggests sliding window. Exact pattern matching suggests KMP, Z or rolling hash; palindrome objectives suggest center expansion, Manacher or DP. Check the objective and monotonicity before selecting a window. |
| Longest/Shortest Segment (`longest-shortest-segment`) | variable-sliding-window, prefix-sum, dynamic-programming | If validity is monotonic as the window grows or shrinks (adding an element can only help or only hurt, never both — e.g. all-positive sums, or a distinct-character-count bound), variable sliding window finds the optimum in O(n). If validity depends on an equality condition over sums that may include negative numbers (e.g. 'longest subarray summing to k'), prefix sum + hash map (tracking the first-seen index per prefix value) is needed instead. Non-monotonic conditions typically fall back to dynamic programming. |
| Maximum/Minimum Segment (`maximum-minimum-segment`) | fixed-sliding-window, kadanes-algorithm, monotonic-stack | Fixed window size k with a sum/average target: fixed sliding window, O(n). Window spans the entire array with the target being the maximum-sum contiguous subarray specifically: Kadane's algorithm, O(n). Sliding-window maximum/minimum (the running max/min as a fixed window slides across the array) needs a monotonic deque/stack to stay O(n) rather than O(n*k) from rescanning each window. |
| Range Sum (`range-sum`) | prefix-sum, difference-array | Many read-only range-sum queries on a static array: precompute a prefix-sum array once for O(1) per query. Many range-update operations (add a value across [l, r]) followed by a final read: a difference array answers each update in O(1) and reconstructs final values with one prefix-sum pass at the end — the inverse use case of plain prefix sum. |
| Consecutive Sequence (`consecutive-sequence`) | hash-set, sort-and-scan | A hash set lets you check, for each number, whether it's the start of a sequence (no predecessor present in the set) and then count forward — amortized O(n) total since each number is visited a bounded number of times. Sorting and scanning for consecutive runs is a simpler O(n log n) alternative when the strict O(n) requirement doesn't matter. |
| Interval Overlap (`interval-overlap`) | merge-intervals, insert-interval, interval-intersection, non-overlapping-intervals, sweep-line | If the task is to collapse a single list into non-overlapping ranges, sort-and-merge (merge-intervals) is the direct fit, O(n log n). If two independent sorted lists must be intersected, use the two-pointer interval-intersection variant instead of merging, O(m+n). If the question is about a count or peak (e.g. "max overlapping at once") rather than the merged ranges themselves, switch to sweep-line with a running counter. If it asks for the fewest removals to eliminate all overlap, treat it as greedy interval scheduling (non-overlapping-intervals) sorted by end time, not by start time. |
| Meeting Scheduling (`meeting-scheduling`) | meeting-rooms, min-heap, sweep-line, maximum-concurrent-events, scheduling-with-heap | For a yes/no feasibility check ("can one person attend all meetings"), sort by start time and check adjacent overlaps, O(n log n), no heap needed. For "minimum rooms/resources required", either sort start and end times separately and two-pointer-sweep them, or push meeting end-times onto a min-heap and reuse a room when the heap's minimum end-time has already passed — both O(n log n). When jobs additionally have priorities or cooldowns (not just start/end times), move to scheduling-with-heap, which pops the highest-priority ready task each tick instead of just tracking room counts. |
| Parentheses Matching (`parentheses-matching`) | stack-based-parentheses-matching, stack | This is a pure last-in-first-out matching problem: push opening symbols, and on a closing symbol pop and check it matches the most recent opener. A stack is essentially the only viable structure here — the LIFO order is exactly what "most recently opened closes first" requires. Variants asking for the *longest* valid substring keep the stack but store indices instead of characters so the gap between mismatches can be measured. |
| Expression Evaluation (`expression-evaluation`) | stack, shunting-yard | Postfix/RPN expressions evaluate directly with a single operand stack, O(n), no precedence logic needed since the notation already encodes order. Infix expressions with precedence and parentheses need either two stacks (operands + operators, applying operators when precedence drops) or a shunting-yard-style conversion to postfix first, then evaluation. Choose the two-stack direct approach for a one-off calculator; choose shunting-yard when the expression must be converted/reused multiple times (e.g. compiled once, evaluated many times). |
| Next Greater Element (`next-greater-element`) | monotonic-decreasing-stack | A monotonic decreasing stack of indices is the direct O(n) fit: scan left to right, and whenever the current value exceeds the stack's top, pop and resolve that index's answer, since indices only ever get popped once. Brute-force nested loops are O(n^2) and should be flagged as suboptimal for any n beyond a few hundred. Circular-array variants iterate the array twice (or index modulo n) over the same monotonic stack. |
| Next Smaller Element (`next-smaller-element`) | monotonic-increasing-stack | Same mechanics as next-greater-element but with a monotonic increasing stack instead of decreasing: pop while the stack's top is greater than or equal to the current value, resolving each popped index's answer as it leaves. O(n) single pass, since each index is pushed and popped at most once. |
| Histogram Problems (`histogram-problems`) | monotonic-increasing-stack, monotonic-decreasing-stack | Largest-rectangle-in-histogram uses a monotonic increasing stack of bar indices: when a shorter bar is encountered, pop taller bars and compute the rectangle each one could have spanned (width = current index - new stack top - 1). This resolves the naive O(n^2) "expand left/right per bar" approach down to O(n). The 2D "maximal rectangle in a binary matrix" variant reduces to this exact histogram problem run once per matrix row, treating each row as histogram heights accumulated from consecutive 1s above it. |
| Stock Span (`stock-span`) | monotonic-decreasing-stack | Maintain a monotonic decreasing stack of (price, span) pairs; on each new price, pop and accumulate the span of every stack entry with price <= today's, then push (today's price, accumulated span + 1). This gives amortized O(1) per call and O(n) total, versus O(n) per call / O(n^2) total for a naive backward scan. Because prices stream in one at a time, this is one of the few next-greater-style problems solved online rather than with a single upfront left-to-right pass. |
| Top K (`top-k`) | min-heap, max-heap, bucket-sort, quickselect | A size-k heap (min-heap for top-k-largest, max-heap for top-k-smallest) runs in O(n log k), stays simple to reason about, and naturally supports streaming input where n isn't known upfront. Quickselect (partition-based) achieves O(n) average time but requires the full dataset in memory upfront (no streaming), mutates the input array, and has O(n^2) worst case without randomized pivoting. Bucket sort by frequency/value is O(n) and simplest of all when the value range (e.g. frequency counts bounded by n) is known and bounded — but it doesn't generalize to arbitrary/unbounded value ranges the way heap or quickselect do. |
| Kth Largest / Kth Smallest (`kth-largest-kth-smallest`) | heap-based-kth-element, quickselect, binary-search-on-answer | For a single one-off query on a static array, quickselect is the fastest average case at O(n), though it mutates the array and degrades to O(n^2) worst case without randomization/median-of-medians. For a stream that keeps growing with repeated kth-largest queries, maintain a size-k min-heap instead — O(log k) per insertion, since quickselect would have to rerun from scratch each time. When the array holds a bounded/discrete value range instead of arbitrary numbers (or the question is phrased as "find the value V such that exactly k elements are <= V" over a monotonic search space, e.g. two sorted arrays), binary-search-on-answer over the value range can beat both, often reaching O(log(range)) or O(log(m+n)). |
| Tree Ancestor Queries (`tree-ancestor-queries`) | binary-lifting, lowest-common-ancestor, heavy-light-decomposition | For a single LCA query (or O(1) of them), a plain recursive DFS is simplest at O(n) per query — no preprocessing needed. For many repeated ancestor/LCA queries on a tree that doesn't change, precompute a binary-lifting table once in O(n log n), then answer each query in O(log n) — this is the standard fit for "kth ancestor" and "LCA" at scale. When queries additionally need aggregates ALONG the path between two nodes (sum/max/min of edge or node weights, not just which node is the LCA), escalate to heavy-light decomposition over a segment tree, O(log^2 n) per path query — binary lifting alone only identifies the LCA, it doesn't aggregate along the path. |
| Island Problems (`island-problems`) | dfs, bfs, flood-fill, union-find | All four candidates correctly count/label islands — the choice is about what else the problem needs. Pure counting on a static grid: DFS or BFS, either works, pick whichever you find easier to write iteratively (DFS risks stack depth on huge grids, so BFS is safer at scale). Need island SIZE per component too: flood fill (DFS/BFS variant that accumulates a counter while marking). Grid changes incrementally over time (e.g. land is added one cell per query and you must answer 'how many islands now' after each addition): union-find, because DFS/BFS would require a full O(rows*cols) re-scan per query while union-find updates in near-constant time per added cell. |
| Connected Groups / Components (`connected-groups`) | connected-components, union-find, dfs, bfs | The relationships are given all at once as a static adjacency list/matrix: plain DFS/BFS traversal counting unvisited-node starts is simplest. Relationships (edges/unions) arrive incrementally, one at a time, and you need to answer 'are X and Y connected' or 'how many groups' after each addition: union-find, because re-running DFS/BFS after every edge would be O(V+E) per query versus near-constant per union. Need to know each node's specific group id afterward for repeated lookups (not just a count): component-labeling (a DFS/BFS pass that assigns a label array) or union-find's find() as the lookup. |
| Dependency Problems (`dependency-problems`) | topological-sort | This shape is almost always topological sort once you confirm the relationships form a directed graph of 'A before B' pairs. The remaining choice is Kahn's (BFS, indegree array — preferred when you also need to detect a cycle as a natural byproduct of 'not all nodes got dequeued', and when recursion depth on a large graph is a concern) versus DFS-based topo sort (preferred when the graph is already being explored recursively for another reason, or the ordering needs to be built via post-order finish times). |
| Course Scheduling (`course-scheduling`) | topological-sort, directed-cycle-detection | Asked only 'can all courses be finished' (a yes/no): directed cycle detection alone suffices, no need to construct the full ordering. Asked for the actual valid course order (or 'one valid order'): full topological sort, which also detects impossibility (a cycle) as a side effect of not being able to produce a complete ordering — so in practice, just running topological sort covers both variants of this shape. |
| Path Finding (`path-finding`) | bfs, dijkstra, bellman-ford, floyd-warshall, a-star | Decide in this order. (1) Are edges unweighted (every move costs the same)? Use BFS — it's the cheapest correct option and anything else is overkill. (2) Are weights present but all non-negative, single source? Use Dijkstra. (3) Can weights be negative, single source? Use Bellman-Ford (and check its negative-cycle flag). (4) Do you need shortest paths between ALL pairs, not just from one source, on a small/dense graph? Use Floyd-Warshall (O(V^3)) rather than running Dijkstra V times unless the graph is sparse enough for that to be cheaper. (5) Is there a single known target and a reliable admissible distance heuristic (e.g. grid Euclidean/Manhattan distance)? A* often prunes the search far faster than Dijkstra in practice, though it never beats Dijkstra's worst case. |
| Minimum Unweighted Steps (`minimum-unweighted-steps`) | bfs | BFS is the standard choice when every move has the same positive cost and the reachable state space is manageable; bidirectional BFS may reduce exploration when reverse moves and a target are available. Ordinary BFS uses layer order as distance order — do not reach for Dijkstra here, since Dijkstra's heap overhead (O(E log V)) buys nothing over BFS's O(V+E) when all weights are equal, and BFS's layer order already equals distance order. Only switch to Dijkstra if the problem quietly introduces different costs per move (e.g. some moves cost 2, or terrain-based costs) — re-read the constraints for that before assuming this shape. |
| Weighted Shortest Path (`weighted-shortest-path`) | dijkstra, bellman-ford, 0-1-bfs, dag-shortest-path, floyd-warshall | The deciding question is: can any edge weight be negative? All weights non-negative → Dijkstra (O(E log V), the faster choice, always preferred when legal). Any edge can be negative, or you must actively detect a negative-weight cycle → Bellman-Ford (O(V*E), slower but correct where Dijkstra silently gives wrong answers). A secondary question: is the graph acyclic (a DAG)? If so, skip both and use DAG shortest path (O(V+E), one topological-order relaxation pass) — it tolerates negative weights and beats both alternatives on a known-acyclic graph. |
| Word Transformation (`word-transformation`) | bfs, bidirectional-bfs | Each word is an implicit graph node, each valid one-letter-change to another dictionary word is an unweighted edge, so plain BFS gives the minimum transformation count. Switch to bidirectional BFS specifically when the dictionary is large and/or the word length makes the per-word branching factor large (26 * word-length candidate neighbors per node) — searching simultaneously from both the start and end word and stopping when frontiers meet cuts the effective search space roughly in half the exponent, which matters once single-direction BFS would visit too many words before reaching the target. |
| Network Capacity (`network-capacity`) | maximum-flow | Once you've confirmed the problem is genuinely about a capacity-constrained network (as opposed to a disguised shortest-path or matching problem), the choice is which max-flow algorithm to implement: Edmonds-Karp (O(V*E^2)) is the simplest correct polynomial choice for small-to-medium graphs; Dinic's (O(V^2 E), O(E*sqrt(V)) on unit networks (such as the standard bipartite-matching reduction)) is worth the extra implementation complexity only when constraints are large. If costs per unit of flow are also given (not just capacities), this is actually min-cost max-flow, a different node, not plain maximum flow. |
| Bipartite Assignment (`bipartite-assignment`) | bipartite-matching, hungarian-algorithm | If every valid pairing is equally good and the goal is simply to maximize the COUNT of matched pairs (unweighted): bipartite matching, implemented via Kuhn's algorithm (O(V*E), fine for interview-sized inputs) or Hopcroft-Karp (O(E*sqrt(V)), needed only at large scale). If each possible pairing carries a different cost/value and the goal is to minimize total cost (or maximize total value) over a full assignment: this is the weighted Assignment Problem, solved by the Hungarian algorithm (O(V^3)) or, equivalently, min-cost max-flow — using plain bipartite matching here would ignore the weights and give a wrong (or merely feasible, not optimal) answer. |
| Overlapping / Conflict Structure (`overlapping-concurrent-structure`) | graph-coloring, bipartite-checking | If the constraint is strictly pairwise 'these two cannot be together' and you only need to know whether TWO groups suffice: bipartite checking (2-coloring) — O(V+E), polynomial, always decidable via BFS/DFS. If more than two groups/colors/time-slots may be needed (k-coloring for k >= 3, e.g. exam-timetabling with more than two slots): general graph coloring, which is NP-complete to solve optimally — expect backtracking with pruning, not a polynomial guarantee, and confirm the problem's constraints (small V) before committing to that approach. |
| Permutations (`permutations-shape`) | permutations, backtracking, bitmask-dp | Use `permutations`/`backtracking` (choose-explore-undo over a used[] set or in-place swapping) when every distinct ordering must actually be enumerated and n is small enough that n! is tractable (roughly n <= 10-12). When the problem only asks for an optimal cost/count over all orderings — not the orderings themselves — and n <= ~20, `bitmask-dp` (state = which elements have been placed) computes the answer in O(2^n * n)-ish time without ever materializing an ordering, which is exponentially cheaper than generating all n! permutations. |
| Combinations (`combinations-shape`) | combinations, combination-sum, backtracking, 01-knapsack, subset-sum | Enumerate every combination itself -> `combinations`/`backtracking` with a start-index guard to avoid order-duplicates. Elements may repeat and must sum exactly to a target while still enumerating every valid combination -> `combination-sum`. When the question only needs whether/how many/the best-value combination of a given size or sum exists — not the actual list of combinations — that's a counting/feasibility/optimization question, which is `01-knapsack`/`subset-sum` DP instead, and is far cheaper than enumerating C(n,k) combinations. |
| Subsets (`subsets-shape`) | backtracking, subset-sum, 01-knapsack, bitmask-dp | Need to actually list every subset -> `backtracking` (include/exclude recursion, sorted + skip-duplicates for 'Subsets II') or an iterative bitmask enumeration over 0..2^n-1. Need only to know whether some subset (or how many subsets) satisfies a sum/count property, without listing them -> DP (`subset-sum` for boolean reachability, `01-knapsack` for a value-optimizing variant) — dramatically cheaper than materializing 2^n subsets. Need the OPTIMAL value achievable for every possible subset composition simultaneously (n <= ~20) -> `bitmask-dp`, where the mask itself is the DP state. |
| Partition Problems (`partition-problems`) | subset-sum, partition-dp, 01-knapsack, backtracking, greedy | Need only feasibility or the optimal sum-difference for a two-group split, with a sum small enough to index (roughly <= 10^4-10^5) -> `subset-sum`/`partition-dp` DP, which reduces the equal-sum case directly to target = totalSum / 2. Need to enumerate ALL valid partitions, split into k > 2 groups with small n, or the partition unit is non-numeric (e.g. palindrome partitioning of a string) -> `backtracking`/`01-knapsack`-style DP over group assignments. A simple sort-then-greedy rule provably yields the optimal split (rare — must verify the greedy-choice property first) -> `greedy`. |
| Scheduling Problems (`scheduling-problems`) | interval-scheduling, greedy, deadline-scheduling, activity-selection, 01-knapsack | Maximize the COUNT of non-overlapping intervals (no per-interval value) -> `interval-scheduling`/`activity-selection`, greedy sorted by end time. Jobs each have a deadline and a profit, one job per slot -> `deadline-scheduling`, greedy sorted by profit descending, placed in the latest free slot. Intervals carry weights/values and a simple greedy order is NOT provably optimal (e.g. weighted interval scheduling, or a capacity/budget constraint beyond 'no overlap') -> DP over intervals sorted by end time, shaped like `01-knapsack` (take-or-skip each interval, skip forward past whatever it conflicts with). |
| Matching Problems (`matching-problems`) | bitmask-dp, bipartite-matching, greedy | Small n (roughly <= 20) and the goal is an optimal-cost/value one-to-one assignment -> `bitmask-dp`, with the mask tracking which elements on one side are already used. General maximum bipartite matching (unweighted, cardinality only, n can be much larger) -> `bipartite-matching` (graphs track — augmenting-path/Hopcroft-Karp based; referenced by id only, minted elsewhere). A simple sorted-greedy pairing provably achieves the optimum (e.g. pairing two sorted arrays to minimize/maximize the sum of paired differences) -> `greedy`, with an exchange-argument proof. |
| State-Space Search (`state-space-search`) | backtracking, word-search, bitmask-dp, bfs | Need to enumerate all paths, or find ANY valid path where choices must be undoable to try alternatives -> `backtracking` (DFS with choose/explore/undo, e.g. `word-search`). Need the SHORTEST sequence of transformations in an unweighted state graph -> `bfs` (graphs track; referenced by id only) explored level by level from the start state. The state naturally compresses into a small set of discrete flags (n <= ~20, e.g. 'which keys collected') and an optimal count/cost over that state space is needed -> `bitmask-dp`. |
| Constraint Satisfaction (`constraint-satisfaction`) | backtracking, constraint-search, n-queens, sudoku-solver, pruning | Default approach is `backtracking` with `constraint-search` (checking validity incrementally at each partial assignment rather than generating everything then filtering). When conflicts can be tracked with O(1)-lookup sets over a small number of constraint dimensions (rows/columns/diagonals) -> `n-queens`-style. When constraints span multiple overlapping groups simultaneously (rows AND columns AND boxes) -> `sudoku-solver`-style, often paired with candidate elimination. Once basic backtracking is too slow for the given constraints, layer `pruning`/branch-and-bound on top rather than switching strategies. |
| Prefix Search (`prefix-search`) | trie, prefix-trie | If queries are only 'does this exact word exist' with no prefix matching, a plain hash set is simpler and just as fast. Reach for a trie specifically when queries need prefix-level matching (starts-with, prefix counting, wildcard search) or when many words share prefixes and you want to avoid re-comparing shared characters repeatedly. |
| Multiple String Pattern Search (`multiple-string-pattern-search`) | aho-corasick | Running KMP or a rolling hash once per pattern costs O(k * (n + m)) for k patterns — fine for a handful of patterns, but Aho-Corasick builds one automaton over all patterns and scans the text once in O(n + total pattern length + matches), which wins decisively once k grows past a small constant. |
| Repeated String Matching (`repeated-string-matching`) | kmp, z-algorithm, rolling-hash, rabin-karp | KMP and the Z algorithm are deterministic O(n + m) with no risk of false positives — prefer them when correctness must be guaranteed and pattern/text sizes are modest. Rolling hash (Rabin-Karp) is simpler to implement and generalizes easily to 2D/multi-pattern variants, but carries a (small, manageable-with-double-hashing) collision risk, so it's the pragmatic choice for quick implementation or when combining hashes across many substrings, not when adversarial inputs are a concern. |
| Palindrome Substring Queries (`palindrome-substring-queries`) | manachers-algorithm, expand-around-center, dynamic-programming, palindromic-tree | For a single 'longest palindromic substring' query, expand-around-center is O(n^2) but simple and usually fast enough; Manacher's algorithm gets the same answer in O(n) when n is large enough to matter. A DP table (palindrome[i][j]) is the right choice when you need O(1) palindrome lookups for many arbitrary (i, j) ranges after O(n^2) preprocessing. Reach for a palindromic tree (eertree) specifically when you need to enumerate or count every distinct palindromic substring, not just the longest one. |
| Range Query (`range-query`) | prefix-sum, fenwick-tree, segment-tree, sparse-table | First ask: does the array change between queries (updates interleaved with queries)? If STATIC (no updates): a 2D/1D prefix sum answers pure sum queries in O(1) after O(n) preprocessing; a sparse table answers min/max/gcd (idempotent) queries in O(1) after O(n log n) preprocessing and is the right default for static range-min/max. If DYNAMIC (point or range updates must be supported between queries): prefix sum and sparse table no longer work efficiently (rebuilding costs O(n) per update) — use a Fenwick tree for point-update/range-sum (simplest to code, O(log n) both ops), or a segment tree when the query is not sum (min/max/gcd/xor) or when both range updates and range queries are needed (segment tree, optionally with lazy propagation). |
| Range Update (`range-update`) | difference-array, lazy-segment-tree | First ask: are queries interleaved with updates, or do all updates happen first and queries (or a single final readout) happen after? If updates are OFFLINE — apply all range updates, then read the final array once (or take a prefix sum at the end) — a difference array does each update in O(1) and reconstructs the final array in O(n) total, no tree needed. If range updates and range QUERIES are interleaved (an update, then a query, then another update, ...) and need to reflect current state each time, a difference array cannot answer point/range reads efficiently mid-stream — use a lazy segment tree instead, which supports O(log n) range update and O(log n) range query in any order. |
| Static Min/Max Query (`static-min-max-query`) | sparse-table | The defining constraint is 'no updates' — if that holds, a sparse table is essentially always the right call for idempotent range aggregates (min, max, gcd, and, or): O(n log n) preprocessing, O(1) per query. If updates are later added to the problem, this stops being this shape — it becomes Dynamic Range Sum / Range Query territory (Fenwick tree or segment tree) instead. |
| Dynamic Range Sum (`dynamic-range-sum`) | fenwick-tree, segment-tree | Both structures solve this in O(log n) per operation. Default to a Fenwick tree when the query is a plain sum (or any operation with a well-defined inverse, so ranges can be derived by subtracting two prefix queries) — it's simpler to code and has a smaller constant factor. Reach for a segment tree instead when the aggregate isn't invertible (min, max, gcd) or when the problem also needs range updates (pair with lazy propagation) or richer combine logic than a Fenwick tree's prefix-based trick can express. |
| Monotonic Answer Space (`monotonic-answer-space`) | binary-search-on-answer | The deciding question is: as the candidate answer X increases, does whether X is 'feasible' change monotonically (all infeasible X below some threshold, all feasible X above it, or vice versa)? If feasibility is monotonic in the answer, binary search the answer space directly: binary search over candidate X values, and for each candidate run an O(check(X)) feasibility test, giving O(check(X) * log(range)) total instead of trying every possible X. If feasibility is NOT monotonic (there can be feasible X sandwiched between infeasible ones), binary search on answer is unsound and a different technique (DP, greedy, search) is required instead. |

## Curriculum QA

The taxonomy is expandable; these counts are a snapshot, not an official number of patterns.
Aliases and parent links keep synonyms and variants connected. Every candidate approach,
parent, prerequisite and problem-shape reference is validated by taxonomy.test.ts.
Skeleton entries advertise no templates. Actual template availability must be checked
against the deep-content registry as authored lessons become available.

Educational corrections include separate substring/subsequence palindrome reasoning,
non-universal sliding-window recognition, and correct flow algorithm assumptions.
Dinic's O(E sqrt V) bound applies to unit networks such as bipartite matching reductions,
not arbitrary unit-capacity graphs. See [the algorithm reference](https://cp-algorithms.com/graph/dinic.html).

## Range-query selection

| Technique | Updates | Queries | Static/dynamic | Complexity |
|---|---|---|---|---|
| Prefix sum | Rebuild after arbitrary changes | Range sum | Static | O(n) build, O(1) query |
| Difference array | Batch range addition | Materialize final values | Offline | O(1) update, O(n) reconstruction |
| Fenwick tree | Point addition; range variants with differences | Prefix/range sum | Dynamic | O(log n) update/query, O(n) storage |
| Segment tree | Point assignment/update | Associative range aggregate | Dynamic | O(n) build, O(log n) update/query |
| Lazy segment tree | Compatible range updates | Range aggregate | Dynamic | O(log n) update/query with composable lazy tags |
| Sparse table | Rebuild | Static idempotent min/max/gcd | Static | O(n log n) build/space, O(1) idempotent query |
| Square-root decomposition | Point updates, block-aware variants | Range aggregate | Dynamic | Typically O(sqrt n) query; update depends on aggregate |
| Mo's algorithm | Offline variants support modifications | Reorder range queries with cheap add/remove | Offline | Typical O((n+q)sqrt n) add/remove operations |
| Ordered set | Insert/delete | Order, predecessor/successor | Dynamic | Balanced-tree O(log n); rank requires augmentation |
| Coordinate compression | Usually preprocess known keys | Rank mapping | Usually offline | O(n log n) preprocessing; does not itself answer aggregates |
