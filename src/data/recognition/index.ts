import type { ConfusionPair, DecisionTreeNode, RecognitionQuestion } from '../../types/domain'

const question = (id: string, prompt: string, clues: string[], correctPatternIds: string[], distractorPatternIds: string[], explanation: string): RecognitionQuestion => ({ id, prompt, clues, correctPatternIds, distractorPatternIds, explanation })

export const recognitionQuestions: RecognitionQuestion[] = [
  question('letters-in-groups', 'Group words made from the same letters. Each word contains only lowercase English letters.', ['group words', 'same letters', 'lowercase'], ['frequency-counting', 'hash-map'], ['sliding-window', 'bfs'], 'Build a 26-count canonical signature for each word, then use a map to collect equal signatures. Sorting each word is a valid O(L log L) alternative to O(L) counting.'),
  question('signed-subarray', 'Count contiguous subarrays whose sum equals a target. Values may be negative.', ['contiguous', 'sum equals', 'negative'], ['prefix-sum', 'hash-map'], ['sliding-window', 'binary-search'], 'Store frequencies of earlier prefix sums. A sum-based shrinking window lacks monotonicity when negative values are allowed.'),
  question('unique-window', 'Find the longest contiguous substring containing no repeated characters.', ['longest', 'substring', 'no repeated'], ['sliding-window', 'hash-map'], ['kadanes-algorithm', 'union-find'], 'Maintain character counts or last positions and move the left boundary until the window has no duplicates. Each boundary moves only forward.'),
  question('negative-roads', 'Find single-source shortest paths in a directed graph with negative edge weights; detect reachable negative cycles.', ['shortest paths', 'negative edge weights', 'negative cycles'], ['bellman-ford'], ['dijkstra', 'bfs'], 'Bellman–Ford allows negative edges and checks for a further relaxation after V−1 passes. A reachable negative cycle can make distances unbounded below.'),
  question('nonnegative-roads', 'Find shortest paths from one vertex in a sparse graph with non-negative weighted edges.', ['one vertex', 'non-negative', 'weighted'], ['dijkstra'], ['bfs', 'kruskal'], 'A min-priority queue expands the smallest tentative distance. Non-negative edges justify finalizing it. A binary heap gives O((V+E) log V).'),
  question('equal-moves', 'Find the minimum moves to a target. Every valid move costs exactly one.', ['minimum moves', 'costs exactly one'], ['bfs'], ['dfs', 'greedy'], 'BFS explores increasing distance layers, so first discovery is shortest for equal-cost edges. DFS does not guarantee shortest routes.'),
  question('mutable-sums', 'An array receives point updates interleaved with range sum queries.', ['point updates', 'interleaved', 'range sum'], ['fenwick-tree', 'segment-tree'], ['prefix-sum', 'sparse-table'], 'Fenwick supports point updates and prefix sums in O(log n); subtract prefixes for ranges. Segment trees are also valid and support more general aggregates.'),
  question('fixed-minimums', 'Answer many range minimum queries on an array that never changes.', ['range minimum', 'never changes'], ['sparse-table'], ['hash-map', 'difference-array'], 'The min operation is idempotent, so overlapping sparse-table blocks answer a query in O(1), after O(n log n) preprocessing. Segment trees trade slower queries for less preprocessing.'),
  question('streaming-top', 'Keep the largest ten values from a stream too large to store in full.', ['largest ten', 'stream', 'cannot store'], ['min-heap'], ['quickselect', 'merge-sort'], 'Maintain a min-heap of size k. Its root is the smallest retained value; replace it when a larger value arrives. O(log k) per item and O(k) memory.'),
  question('one-shot-rank', 'Find one kth smallest value in an in-memory array. Mutation is permitted; expected linear time is acceptable.', ['kth smallest', 'in-memory', 'expected linear'], ['quickselect'], ['bfs', 'trie'], 'Quickselect partitions and searches only the relevant side. Randomization gives expected O(n), but its worst case remains O(n²).'),
  question('build-order', 'Return a valid build order when each task lists prerequisites, or report a cycle.', ['build order', 'prerequisites', 'cycle'], ['topological-sort'], ['union-find', 'dijkstra'], 'Model prerequisite edges as directed. Kahn’s algorithm consumes indegree-zero tasks; fewer than V consumed tasks signals a cycle.'),
  question('joining-groups', 'Undirected connections are added over time. After each addition, determine whether two vertices belong to the same group.', ['connections added', 'same group'], ['union-find'], ['topological-sort', 'binary-search'], 'DSU maintains merged components with near-constant amortized operations. Ordinary DSU does not support arbitrary edge deletion.'),
  question('warmer-next', 'For each day, find the next day with a strictly higher temperature.', ['next', 'strictly higher'], ['monotonic-stack'], ['heap', 'prefix-sum'], 'A decreasing stack keeps unresolved indices. A warmer value resolves smaller previous values; each index is pushed and popped at most once.'),
  question('all-small-orders', 'List every ordering of ten distinct objects.', ['every ordering', 'ten'], ['backtracking'], ['greedy', 'binary-search'], 'Choose an unused object, recurse, and undo. There are 10! outputs; even a correct generator must pay for writing them. Factorial work is only plausible at small sizes.'),
  question('size-twenty', 'Choose a subset of 20 tasks; feasibility depends on the exact set already selected.', ['subset', '20', 'exact set'], ['bitmask-dp'], ['sliding-window', 'two-pointers'], 'A mask can represent 2^20 subsets, though multiplying by transitions and memory per state matters. Small n suggests exponential methods, not automatic feasibility.'),
  question('size-forty', 'Given 40 arbitrary integers, find a subset with a target sum. Magnitudes are too large for sum-indexed DP.', ['40', 'subset', 'large magnitudes'], ['meet-in-the-middle'], ['counting-array', 'sliding-window'], 'Split into halves, enumerate roughly 2^20 sums each, then match complements. O(2^(n/2)) storage is still significant.'),
  question('size-large', 'Process 100,000 values under a typical time limit. Which growth rates should you investigate first?', ['100,000', 'time limit'], ['linear-scan', 'sort-and-scan'], ['backtracking', 'bubble-sort'], 'Linear and n log n approaches are common targets. This is a heuristic: operation cost, hardware, output size, and time budget still matter.'),
  question('prefix-dictionary', 'Repeatedly list dictionary entries beginning with a supplied prefix.', ['dictionary', 'prefix'], ['trie'], ['union-find', 'heap'], 'A trie follows prefix characters before traversing matching descendants. Total work also includes the output size; a hash map alone supports exact lookup, not prefix navigation.'),
  question('all-patterns', 'Scan one long text for occurrences of thousands of fixed dictionary patterns.', ['long text', 'thousands', 'patterns'], ['aho-corasick'], ['two-pointers', 'binary-search'], 'Aho–Corasick combines a trie and failure links to scan for multiple patterns; account for alphabet representation and number of reported matches.'),
  question('ancestor-queries', 'Answer many kth-ancestor queries on a static rooted tree.', ['kth-ancestor', 'static tree'], ['binary-lifting'], ['heap', 'sliding-window'], 'Precompute 2^j-th ancestors. Decompose k into set bits for O(log n) queries after O(n log n) preprocessing.'),
  question('feasibility-threshold', 'Find the smallest machine capacity that meets a deadline. Increasing capacity never makes feasibility worse.', ['smallest capacity', 'never makes feasibility worse'], ['binary-search-on-answer'], ['dfs', 'quickselect'], 'The feasibility predicate has a single threshold. Binary search candidates, multiplying search iterations by the cost of each feasibility check.'),
]

const pair = (id: string, patternIdA: string, patternIdB: string, distinguishingQuestion: string, guidance: string): ConfusionPair => ({ id, patternIdA, patternIdB, distinguishingQuestion, guidance })
export const confusionPairs: ConfusionPair[] = [
  pair('hash-or-sort', 'hash-map', 'sort-and-scan', 'Do you need original indices or ordered traversal?', 'Hashing offers expected O(n) lookup-based processing with extra memory. Sorting costs O(n log n) but exposes adjacency and ordered structure; preserve indices explicitly if required.'),
  pair('pointers-or-window', 'two-pointers', 'sliding-window', 'Are the pointers maintaining an entire contiguous candidate interval?', 'Sliding window is a two-pointer specialization maintaining interval state. Opposite-end two pointers instead often eliminate pairs using sorted order.'),
  pair('prefix-or-window', 'prefix-sum', 'sliding-window', 'Can moving a boundary change validity predictably?', 'For target-sum counting with negatives, use prefix sums and a frequency map. Positive-sum bounds can support shrinking windows; character-count windows have different validity invariants.'),
  pair('bfs-or-dfs', 'bfs', 'dfs', 'Do you need minimum equal-cost distance or depth-oriented exploration?', 'Both traverse components in O(V+E). BFS gives shortest equal-cost paths; DFS suits finish-order reasoning, recursion, and backtracking. Use explicit stacks when recursion depth is unsafe.'),
  pair('bfs-or-dijkstra', 'bfs', 'dijkstra', 'Do all edges cost the same?', 'BFS suffices for equal weights; Dijkstra handles differing non-negative weights. For weights restricted to 0 and 1, a deque-based 0–1 BFS achieves O(V+E).'),
  pair('dijkstra-or-bellman', 'dijkstra', 'bellman-ford', 'Can reachable edges have negative weights?', 'Dijkstra’s greedy finalization requires non-negative edges. Bellman–Ford handles negatives and detects reachable negative cycles. A known DAG permits topological relaxation even with negatives.'),
  pair('heap-or-select', 'heap', 'quickselect', 'Is the input streaming or a mutable one-shot batch?', 'Use a bounded heap for streaming top-k, O(n log k), O(k) space. Quickselect is expected O(n) for one in-memory selection but has O(n²) worst-case time and does not keep results sorted.'),
  pair('greedy-or-dp', 'greedy', 'dynamic-programming', 'Can you prove that a local choice preserves an optimal solution?', 'Greedy needs an exchange argument or another correctness proof. DP models alternatives with sufficient state when subproblems overlap; apparent simplicity is not proof that greedy works.'),
  pair('search-or-dp', 'backtracking', 'dynamic-programming', 'Do different choice histories reach an equivalent remaining problem?', 'Backtracking enumerates choices and prunes invalid branches. Memoization merges equivalent states, but listing every solution still requires output-sensitive work.'),
  pair('traversal-or-dsu', 'dfs', 'union-find', 'Is connectivity static, or are edges repeatedly added?', 'DFS/BFS labels static components and can recover paths. DSU handles incremental undirected connectivity, but does not preserve paths or support ordinary arbitrary deletions.'),
  pair('segment-or-fenwick', 'segment-tree', 'fenwick-tree', 'Can range answers be derived by subtracting prefix aggregates?', 'Fenwick is compact for point-update range sums. Segment trees support associative aggregates such as min; compatible lazy actions extend them to range updates.'),
  pair('trie-or-map', 'trie', 'hash-map', 'Do queries ask about prefixes or exact keys?', 'Hash maps are simple for exact membership. Tries share prefixes and support character-wise navigation. Both have memory and alphabet representation tradeoffs.'),
  pair('topo-or-dfs', 'topological-sort', 'dfs', 'Do you need an ordering respecting directed prerequisites?', 'Topological sorting is the task; DFS is one way to implement it using finish order and active-state cycle detection. An arbitrary DFS visitation order is not a valid topological order.'),
  pair('subarray-or-subsequence', 'subarray', 'subsequence', 'Must selected positions be contiguous?', 'Subarrays and substrings are contiguous. Subsequences preserve relative order but may skip positions. Subsets disregard sequence order. These are problem shapes, not algorithms.'),
]

export const decisionTree: DecisionTreeNode[] = [
  { id: 'start', question: 'Which structure best describes the task?', branches: { 'Shortest route': 'weights', 'Range queries': 'updates', 'Contiguous target sum': 'signs', 'Prerequisite order': 'pattern:topological-sort', 'Same letters': 'pattern:frequency-counting' } },
  { id: 'weights', question: 'What edge-weight guarantee is available?', branches: { 'Equal cost': 'pattern:bfs', 'Only zero or one': 'pattern:0-1-bfs', 'Non-negative': 'pattern:dijkstra', 'May be negative': 'pattern:bellman-ford', 'Known DAG, any sign': 'pattern:dag-shortest-path' } },
  { id: 'updates', question: 'What changes between queries?', branches: { 'Static sums': 'pattern:prefix-sum', 'Static min/max': 'pattern:sparse-table', 'Point updates and sums': 'pattern:fenwick-tree', 'Interleaved range updates': 'pattern:lazy-segment-tree' } },
  { id: 'signs', question: 'What is the objective and sign constraint?', branches: { 'Count exact sums with negatives': 'pattern:prefix-sum', 'Bounded sum, positive values': 'pattern:sliding-window', 'Maximum sum, any sign': 'pattern:kadanes-algorithm' } },
]
